# Full replacement: env.sample
# copy to .env or export the variables in your shell before starting services

# REST API / Fabric
CHAINCODE_NAME=carboncc
CHANNEL_NAME=mychannel
TLS_CERT_PATH=~/carbonpasture-kenya/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/tls/ca.crt
MSP_ID=Org1MSP
KEY_PATH=~/carbonpasture-kenya/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/users/Admin@org1.example.com/msp/keystore/xxxx_sk
PEER_ENDPOINT=127.0.0.1:7051
PORT=3000

# USSD
USSD_PORT=4000
API_URL=http://127.0.0.1:3000

# Keys (example: generate with openssl rand -hex 32)
# USSD_HASH_KEY: HMAC key to hash phone numbers for logs
# USSD_ENC_KEY: AES-256 key (hex, 32 bytes => 64 hex chars) for encrypting phone→farm mapping
# Example generation:
#  export USSD_HASH_KEY=$(openssl rand -hex 32)
#  export USSD_ENC_KEY=$(openssl rand -hex 32)
USSD_HASH_KEY=
USSD_ENC_KEY=

